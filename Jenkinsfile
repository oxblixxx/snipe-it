pipeline {
    agent any

    stages {
        stage('Check GitHub Access') {
            steps {
                script {
                    echo "Successfully checked out the GitHub repository."
                }
            }
        }
    }
}
